<li>
    <div class="form-group">
        <input type="checkbox"  value="{{ $volume->id }}" id="volumeChb{{ $volume->id }}" class="liquor-volume" name="liquor-volume">
        <label for="volumeChb{{ $volume->id }}">{{ $volume->volume.' l' }}</label>
    </div>
</li>
